@import '../constants/_goldenratios.styl'

nav()
    nav
        ul > li > a
            display block
            padding-top 1em
            padding-bottom 0
            padding-left unit($phi, em)
            padding-right unit($phi, em)
        ul > li:last-child
            a
                padding-bottom 1em
